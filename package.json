{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "postbuild": "node postBuild.mjs",
    "dev": "nuxt dev",
    "predeploy": "npm install --prefix .output/server",
    "deploy": "firebase deploy --only hosting,functions",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {},
  "devDependencies": {
    "@nuxtjs/tailwindcss": "5.3.3",
    "firebase-admin": "11.0.1",
    "firebase-functions": "3.23.0",
    "firebase-functions-test": "2.4.0",
    "nuxt": "3.0.0-rc.10"
  },
  "engines": {
    "node": "16"
  }
}
